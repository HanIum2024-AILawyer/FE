import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);

// If you had code like this, remove it
// import reportWebVitals from './reportWebVitals';
// reportWebVitals(console.log);
